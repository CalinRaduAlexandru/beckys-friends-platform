(function () {
  const roles = [
    ['experienta-copilului', 'Experiența copilului'], ['relatia-cu-parintii', 'Relația cu părinții'],
    ['design-pedagogic', 'Design pedagogic'], ['cultura-experienta-becky', 'Cultura & experiența Becky'],
    ['marketing-comunicare', 'Marketing & comunicare'], ['sisteme-tehnologie', 'Sisteme & tehnologie'],
    ['operatiuni-logistica', 'Operațiuni & logistică'], ['strategie-dezvoltare', 'Strategie & dezvoltare']
  ];
  const sections = [['objectives', 'OBIECTIVELE LUNII', 'Ce mi-am propus concret.'], ['metrics', 'METRICI', 'Cum voi urmări concret progresul.'], ['done', 'CE AM FĂCUT', 'Acțiuni și implementări.'], ['evidence', 'DOVEZI / REZULTATE', 'Numere, feedback, observații, livrabile, teste.'], ['learned', 'CE AM ÎNVĂȚAT', 'Ce a funcționat / nu a funcționat și de ce cred asta.'], ['next_step', 'URMĂTORUL PAS', 'Ce rezultă logic pentru luna următoare.']];
  sections.unshift(['scope', 'SCOP', 'Direcția și rolul acestui domeniu în luna curentă.']);
  const statuses = ['În parametri', 'Necesită atenție', 'În urmă', 'Fără suficiente date'];
  const entryTypes = [['done', 'Făcut'], ['evidence', 'Dovadă'], ['learned', 'Învățare']];
  const esc = value => String(value ?? '').replace(/[&<>"']/g, c => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', '"':'&quot;', "'":'&#039;' }[c]));
  const api = (url, options) => fetch(url, { credentials: 'same-origin', ...options });
  const filled = role => sections.filter(([key]) => String(role.sections?.[key] || '').trim()).length;
  const today = () => new Date().toISOString().slice(0, 10);
  function daysUntil(date) { const now = new Date(); now.setHours(0,0,0,0); return Math.ceil((new Date(`${date}T00:00:00`) - now) / 86400000); }
  function roleFromQuery(report) { const id = new URLSearchParams(location.search).get('role'); return report.roles.find(role => role.id === id)?.id || report.roles[0]?.id; }
  function renderNotes(report) {
    const app = document.querySelector('#app') || document.querySelector('main');
    app.innerHTML = `<section class="monthly-report-shell"><div class="monthly-report-header"><div><small class="monthly-report-eyebrow">ADMIN · RAPORT LUNAR V1</small><h1>Note zilnice</h1><p>Un jurnal comun pentru toate rolurile. Poți folosi <strong>@rol</strong> ca să marchezi contextul unei note.</p></div><a class="monthly-report-back" href="/admin?view=monthly-report">← Înapoi la roluri</a></div><div class="monthly-report-notes-layout"><form class="monthly-report-editor monthly-report-note-form"><label>Data<input id="daily-note-date" type="date" value="${today()}"></label><label>Notă zilnică<textarea id="daily-note-text" placeholder="Ex. @design-pedagogic Am testat..."></textarea></label><div id="daily-note-analysis-state"></div><div class="monthly-report-actions"><span id="daily-note-message"></span><button type="button" id="daily-note-analyze">✨ Analizează nota</button><button class="primary" type="submit">Salvează nota</button></div></form><section class="monthly-report-editor"><div class="monthly-report-editor-head"><h2>Jurnal</h2><a class="monthly-report-tab-link" href="/admin?view=becky-inbox">Deschide Becky Inbox →</a></div><div class="monthly-report-notes-list">${Object.entries(report.notes || {}).sort((a,b) => b[0].localeCompare(a[0])).map(([date,text]) => `<article tabindex="0" data-note-date="${esc(date)}"><time>${esc(date)}</time><p>${esc(text)}</p></article>`).join('') || '<p class="monthly-report-empty">Nu există încă note.</p>'}</div></section></div></section>`;
    let inboxProposals=[];
    const refreshAnalysisState = date => { const host=app.querySelector('#daily-note-analysis-state');const button=app.querySelector('#daily-note-analyze');if(!host||!button)return;const items=inboxProposals.filter(item=>item.source_id===date&&!item.stale);if(!items.length){host.innerHTML='';button.hidden=false;return;}const count=status=>items.filter(item=>item.status===status).length;host.innerHTML=`<div class="monthly-note-analysis-summary"><strong>✨ ${items.length} ${items.length===1?'lucru găsit':'lucruri găsite'}</strong><span>${count('pending')} de verificat · ${count('approved')} păstrate · ${count('ignored')} ignorate${count('failed')?` · ${count('failed')} necesită atenție`:''}</span><a href="/admin?view=becky-inbox&source_id=${encodeURIComponent(date)}">Deschide Becky Inbox →</a></div>`;button.hidden=true;};
    const loadNote = date => { app.querySelector('#daily-note-date').value = date; app.querySelector('#daily-note-text').value = report.notes?.[date] || ''; refreshAnalysisState(date); app.querySelector('#daily-note-text').focus(); };
    app.querySelector('#daily-note-text').value = report.notes?.[today()] || '';
    app.querySelector('#daily-note-date').addEventListener('change', e => loadNote(e.target.value));
    app.querySelectorAll('[data-note-date]').forEach(note => { note.addEventListener('click', () => loadNote(note.dataset.noteDate)); note.addEventListener('keydown', e => { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); loadNote(note.dataset.noteDate); } }); });
    app.querySelector('.monthly-report-note-form').addEventListener('submit', async e => { e.preventDefault(); const date = app.querySelector('#daily-note-date').value; const text = app.querySelector('#daily-note-text').value; const response = await api('/api/admin/monthly-report/notes', { method:'PATCH', headers:{'Content-Type':'application/json'}, body:JSON.stringify({date, text}) }); if (!response.ok) return; report.notes = (await response.json()).notes || report.notes; renderNotes(report); });
    app.querySelector('#daily-note-analyze').addEventListener('click', async () => { const button=app.querySelector('#daily-note-analyze');const message=app.querySelector('#daily-note-message');const date=app.querySelector('#daily-note-date').value;const text=app.querySelector('#daily-note-text').value.trim();if(!text){message.textContent='Scrie nota înainte de analiză.';return;}button.disabled=true;message.textContent='Salvez și analizez nota…';const saved=await api('/api/admin/monthly-report/notes',{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({date,text})});if(!saved.ok){message.textContent='Nota nu a putut fi salvată.';button.disabled=false;return;}const response=await api('/api/admin/becky-inbox/analyze',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({date})});const result=await response.json();if(!response.ok){message.textContent=result.error||'Analiza nu a reușit.';button.disabled=false;return;}location.href=`/admin?view=becky-inbox&source_id=${encodeURIComponent(date)}`; });
    api('/api/admin/becky-inbox/proposals').then(response=>response.ok?response.json():{proposals:[]}).then(payload=>{inboxProposals=payload.proposals||[];refreshAnalysisState(app.querySelector('#daily-note-date').value);});
  }
  function entryRolesHtml(selectedIds) { return roles.map(([id, label]) => `<label class="monthly-entry-role"><input type="checkbox" name="monthly-entry-role" value="${id}" ${selectedIds.includes(id) ? 'checked' : ''}><span>${esc(label)}</span></label>`).join(''); }
  function entryTypeLabel(type) { return entryTypes.find(item => item[0] === type)?.[1] || type; }
  async function renderEntries(app, report, selected) {
    const host = app.querySelector('.monthly-report-entries'); if (!host) return;
    host.innerHTML = '<p class="monthly-report-empty">Se încarcă intrările…</p>';
    const response = await api(`/api/admin/monthly-report/entries?month_key=${encodeURIComponent(report.month_key)}&role_id=${encodeURIComponent(selected.id)}`);
    if (!response.ok) { host.innerHTML = '<p class="monthly-report-empty">Intrările nu sunt disponibile momentan.</p>'; return; }
    const entries = (await response.json()).entries || [];
    host.innerHTML = `<div class="monthly-report-entries-head"><div><span>INTRĂRI ALE LUNII</span><small>Memorie granulară, separată de sinteza rolului.</small></div><button class="monthly-entry-add" type="button">＋ Adaugă intrare</button></div>${entries.length ? `<div class="monthly-report-entry-list">${entries.map(entry => { const others = entry.role_ids.filter(id => id !== selected.id).map(id => roles.find(role => role[0] === id)?.[1]).filter(Boolean); return `<article class="monthly-report-entry" data-entry-id="${esc(entry.id)}"><div class="monthly-entry-meta"><time>${esc(entry.entry_date)}</time><b class="entry-type-${esc(entry.type)}">${esc(entryTypeLabel(entry.type))}</b>${others.length ? `<span>Și: ${esc(others.join(', '))}</span>` : ''}</div><p>${esc(entry.text)}</p><div class="monthly-entry-actions"><button type="button" data-entry-edit="${esc(entry.id)}">Editează</button><button type="button" data-entry-delete="${esc(entry.id)}">Șterge</button></div></article>`; }).join('')}</div>` : '<p class="monthly-report-empty">Nu există încă intrări pentru acest rol.</p>'}`;
    const openForm = entry => {
      const editing = Boolean(entry); const selectedIds = entry?.role_ids || [selected.id];
      const modal = document.createElement('div'); modal.className = 'monthly-entry-modal'; modal.innerHTML = `<div class="monthly-entry-dialog" role="dialog" aria-modal="true"><div class="monthly-entry-dialog-head"><h3>${editing ? 'Editează intrarea' : 'Adaugă intrare'}</h3><button type="button" data-entry-close aria-label="Închide">×</button></div><form><label>Data<input required type="date" name="entry_date" value="${esc(entry?.entry_date || today())}"></label><label>Tip<select name="type">${entryTypes.map(([value,label]) => `<option value="${value}" ${entry?.type === value ? 'selected' : ''}>${label}</option>`).join('')}</select></label><fieldset><legend>Roluri</legend>${entryRolesHtml(selectedIds)}</fieldset><label>Text<textarea required name="text" maxlength="5000" placeholder="Scrie o intrare clară și datată…">${esc(entry?.text || '')}</textarea></label><div class="monthly-report-actions"><span class="monthly-entry-form-message"></span><button class="primary" type="submit">Salvează</button></div></form></div>`; document.body.appendChild(modal);
      const close = () => modal.remove(); modal.querySelector('[data-entry-close]').addEventListener('click', close); modal.addEventListener('click', e => { if (e.target === modal) close(); });
      modal.querySelector('form').addEventListener('submit', async e => { e.preventDefault(); const form = e.currentTarget; const roleIds = [...form.querySelectorAll('input[name="monthly-entry-role"]:checked')].map(input => input.value); const message = form.querySelector('.monthly-entry-form-message'); if (!roleIds.length) { message.textContent = 'Alege cel puțin un rol.'; return; } const payload = { month_key: report.month_key, entry_date: form.querySelector('[name="entry_date"]').value, type: form.querySelector('[name="type"]').value, role_ids: roleIds, text: form.querySelector('[name="text"]').value }; const response = await api(editing ? `/api/admin/monthly-report/entries/${encodeURIComponent(entry.id)}` : '/api/admin/monthly-report/entries', { method: editing ? 'PATCH' : 'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify(payload) }); if (!response.ok) { message.textContent = 'Nu am putut salva intrarea.'; return; } close(); renderEntries(app, report, selected); });
    };
    host.querySelector('.monthly-entry-add').addEventListener('click', () => openForm());
    host.querySelectorAll('[data-entry-edit]').forEach(button => button.addEventListener('click', () => openForm(entries.find(entry => entry.id === button.dataset.entryEdit))));
    host.querySelectorAll('[data-entry-delete]').forEach(button => button.addEventListener('click', async () => { if (!window.confirm('Ștergi această intrare?')) return; const response = await api(`/api/admin/monthly-report/entries/${encodeURIComponent(button.dataset.entryDelete)}`, { method:'DELETE' }); if (response.ok) renderEntries(app, report, selected); }));
  }
  function render(report, selectedId) {
    const total = report.roles.length * sections.length; const complete = report.roles.reduce((sum, role) => sum + filled(role), 0); const days = daysUntil(report.due_date); const selected = report.roles.find(role => role.id === selectedId) || report.roles[0];
    document.body.dataset.workspace = 'monthly-report'; document.querySelectorAll('.sidebar .side-link').forEach(link => link.classList.toggle('active', link.classList.contains('monthly-report-link')));
    const app = document.querySelector('#app') || document.querySelector('main') || document.body;
    app.innerHTML = `<section class="monthly-report-shell"><div class="monthly-report-header"><div><small class="monthly-report-eyebrow">ADMIN · RAPORT LUNAR V1</small><h1>Raport Lunar</h1><p>Un spațiu simplu pentru a nota ce ai urmărit, ce ai făcut și care este următorul pas.</p></div><div class="monthly-report-deadline"><strong>${days >= 0 ? `Mai sunt ${days} zile` : `Termen depășit cu ${Math.abs(days)} zile`}</strong><span>până la trimitere · 2 septembrie</span></div></div><div class="monthly-report-progress"><strong>${complete} / ${total} secțiuni completate</strong><div><i style="width:${total ? (complete / total) * 100 : 0}%"></i></div></div><div class="monthly-report-layout"><nav class="monthly-report-roles"><small>ROLURI</small>${report.roles.map(role => `<a class="monthly-report-role ${role.id === selected.id ? 'is-selected' : ''}" href="/admin?view=monthly-report&role=${encodeURIComponent(role.id)}"><span>${esc(role.label)}</span><em class="status-${statuses.indexOf(role.status)}">${esc(role.status)}</em><b>${filled(role)}/7</b></a>`).join('')}</nav><article class="monthly-report-editor"><div class="monthly-report-editor-head"><div><small>ROL SELECTAT</small><h2>${esc(selected.label)}</h2></div><label>Status<select id="monthly-status">${statuses.map(status => `<option ${status === selected.status ? 'selected' : ''}>${esc(status)}</option>`).join('')}</select></label></div><div class="monthly-report-fields">${sections.map(([key, label, hint]) => `<label><span>${label}</span><small>${hint}</small><textarea data-section="${key}" placeholder="Scrie aici..."></textarea></label>`).join('')}</div><div class="monthly-report-actions"><span id="monthly-save-message"></span><button class="primary" id="monthly-save">Salvează rolul</button></div><section class="monthly-report-entries" aria-labelledby="monthly-entries-title"></section></article></div></section>`;
    app.querySelector('.monthly-report-header')?.insertAdjacentHTML('beforeend', '<a class="monthly-report-tab-link" href="/admin?view=monthly-report&tab=notes">Note zilnice</a>');
    sections.forEach(([key]) => { const field = app.querySelector(`[data-section="${key}"]`); if (field) field.value = selected.sections?.[key] || ''; });
    app.querySelector('#monthly-save').addEventListener('click', async () => { const button = app.querySelector('#monthly-save'); button.disabled = true; const body = { status: app.querySelector('#monthly-status').value, sections: Object.fromEntries(sections.map(([key]) => [key, app.querySelector(`[data-section="${key}"]`).value])) }; try { const response = await api(`/api/admin/monthly-report/roles/${encodeURIComponent(selected.id)}`, { method:'PATCH', headers:{'Content-Type':'application/json'}, body:JSON.stringify(body) }); if (!response.ok) throw new Error(); const result = await response.json(); render({ ...report, roles: report.roles.map(role => role.id === selected.id ? result.role : role) }, selected.id); } catch { const message = app.querySelector('#monthly-save-message'); if (message) message.textContent = 'Nu am putut salva. Încearcă din nou.'; button.disabled = false; } });
    renderEntries(app, report, selected);
  }
  window.renderMonthlyReportAdmin = async function () { const app = document.querySelector('#app') || document.querySelector('main'); if (app) app.classList.add('monthly-report-app'); const response = await api('/api/admin/monthly-report'); if (!response.ok) { if (app) app.innerHTML = '<section class="monthly-report-error">Raportul lunar nu este disponibil momentan.</section>'; return; } const data = await response.json(); if (new URLSearchParams(location.search).get('tab') === 'notes') renderNotes(data.report); else render(data.report, roleFromQuery(data.report)); };
})();
